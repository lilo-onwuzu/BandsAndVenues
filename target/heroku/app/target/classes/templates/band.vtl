<h4>$band.getName()</h4><br>

#if ($venues.size() != 0)
  #foreach ($venue in $venues)
    <a class="list" href="/venues/$venue.getId()">$venue.getName()</a><br>
  #end
#else
  <p><em class="list">No venues have been added for $band.getName()!</em><p>
#end

<br><br><a href="/bands/$band.getId()/remove">Delete $band.getName()</a><br>
<br><h3>Edit Band Name</h3>
<form action="/bands/$band.getId()/edit" method="post">
  <label for="editBand">New Band Name:</label>
  <input id="editBand" name="editBand" type="text">
  <button type="submit" class="btn btn-info">Edit</button>
</form>

<br><a href="/bands">All Bands</a>
<strong class="clickToView">|</strong>
<a href="/venues">All Venues</a><br>
<a href="/bands/$band.getId()/add_venue">Add a Venue For $band.getName()</a>
