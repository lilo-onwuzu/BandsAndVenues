#if ($bands.size() != 0)
  #foreach ($band in $bands)
    <a class="list" href="/bands/$band.getId()">$band.getName()</a><br><br>
  #end
#else
  <p><em class="list">No bands have been added to the list!</em><p>
#end

<br>
<a href="/bands/new">Add New Band  </a>
<a href="/venues">  All Venues</a>
